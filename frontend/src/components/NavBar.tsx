import React from "react";

// create switchable buttons that returns value to main App
// and shows different screns without router.

const NavBar = () => {
  return (
    <div>
      <button>Create</button>
      <button>Select</button>
    </div>
  );
};

export default NavBar;
